~~1.优化类结构~~
4.BSSRDF
5.优化场景的加载
6.模型的法向统一
7.模型对于硬边的处理
9.将场景分为TLAS/BLAS 使得场景可以动态更新 无需重复构建bvh
10.改进 Sobol 序列：使用低差异序列（如 Sobol）代替纯随机采样可加快收敛。但要避免常见误区：不要对 Sobol 序列做 Cranley–Patterson 旋转或像素间随机偏移，这会反而增加方差。最佳做法是对 Sobol 序列进行随机数位扰动（如 Owen 打乱或数字异或），此方法保持了 Sobol 的分布优点并进一步降低方差。例如文献指出，Owen 扰动后的 Sobol 比原始 Sobol 收敛更快。具体可参考 Owen 漏洩 (Digital scrambling) 技术，在每个像素使用不同的扰动常量。
11.sobel可以预计算双倍提升
12.实现降噪
~~13.需修改分块渲染的逻辑~~
14.分离降噪部分
~~15.优化不同的更新操作~~
~~16.之后所有的参数使用统一的单例存储~~

## 当前优先修改

17.修复 OBJ 读取后的模型归一化包围盒计算 bug。`Mesh.cpp` 中 `maxy/maxz/miny/minz` 目前错误地和 `maxx/minx` 混用，非立方模型会被错误缩放，进一步影响 BVH、法线和最终渲染结果。
18.为表面反射/折射后的新光线加入稳定的 ray epsilon 偏移，避免自交、漏光和透明物体重复命中；不要只依赖三角形求交里的 `uvt.z` 阈值。
19.修正 OIDN 辅助法线输入范围。当前 normal buffer 传给 OIDN 前仍是 `[0, 1]` 编码，应改为 `[-1, 1]` 的世界空间或视空间法线。
20.重新检查透明材质与体积路径的首个可见 hit 记录逻辑，确保折射路径和体积路径输出给 OIDN 的 normal/albedo 更稳定。

## 需要增加的功能

21.补全 HDR 环境贴图的重要性采样与 `env vs BRDF` 的 MIS。现在 cache、`SampleHdr`、`hdrPdf` 已有，但直采样路径还没真正接入。
22.实现 Next Event Estimation（NEE），不要只靠路径随机碰撞到发光面；至少先支持 emissive triangle 的显式采样。
23.补齐解析光源系统，支持矩形光、球光、方向光，并与 BRDF 采样统一做 MIS。
24.补全 UV、tangent、bitangent 管线。当前 BSDF 里已经有 TODO，但 mesh 数据结构和编码缓冲里还没有对应字段。
25.支持贴图材质：baseColor、metallic-roughness、normal、emission，避免材质只能靠常量参数表达。
26.补充 alpha cutoff / stochastic alpha testing，使透明叶片、栅栏等材质能按参考实现正确工作。
27.完善 homogeneous volume 实现，引入 medium stack，支持嵌套介质，而不是只用单个 `inMedium` 状态。
28.支持实例化场景资源组织，与已有 TLAS/BLAS 待办结合，减少动态更新时的 BVH 重建成本。
29.补充更完整的资源格式支持，优先考虑 glTF/GLB，减少 OBJ-only 带来的材质和切线信息缺失。
30.增加 preview / final 两种渲染路径：预览模式优先交互响应，最终模式优先累计质量和降噪质量。
31.实现NEE + BSDF 采样 + power heuristic