# learnQT 协作约定

适用于本仓库的所有任务。本文中的“线程”指与用户协作的任务/会话，不是程序中的渲染线程。

## 任务开始：先了解整个项目

每个新任务开始时，在深入代码、制定实现方案或修改文件前，先按顺序完整阅读：

1. [项目文档入口](miscellaneous/project_docs_index.md)：文档分工与统一术语。
2. [项目架构](miscellaneous/project_architecture.md)：模块、线程边界、共享状态与依赖。
3. [渲染流程](miscellaneous/render_flow.md)：启动、渲染、交互和场景切换流程。
4. [实现逻辑](miscellaneous/logic_overview.md)：实际行为、数据链路与限制。
5. [代码地图](miscellaneous/module_map.md)：代码入口和改动影响范围。
6. [待办](miscellaneous/待办.md)：已完成事项、已知问题与后续优先级。
7. [纹理与场景系统 v1](miscellaneous/texture_scene_v1.md)：当前功能范围、预设、验收基线与兼容性边界。

随后按文档入口补读与本次任务相关的专题资料和代码，并检查 `git status`，辨别已有改动。
不要只凭任务标题、旧会话记忆或某一处代码推断整个项目的工作方式。

- 简短说明对本次任务涉及模块和影响范围的理解，再开展工作，不必逐篇复述文档。
- 文档可能滞后；涉及本次任务的行为必须与当前代码核对，不能把历史验收记录当作本轮测试结果。
- 文件缺失或文档与实现冲突时，如实说明，以当前代码和实际验证为依据，不编造已读内容。
- 同一线程延续任务时可复用已经读过的上下文；上下文丢失、分支切换或文档变化后补读相关内容。

## 开发中：不自动执行文档收尾

日常任务按用户要求完成实现与必要验证。即使任务已基本完成，也不要自行进入文档收尾阶段，
不要在每次代码修改后自动更新项目说明或勾选待办。可在回复中记录待同步的文档点和遗留问题。

只有用户明确要求“收尾”“更新文档和待办”等时，才执行下述收尾工作。
单纯询问进度、询问是否完成，或没有对应收尾提议的“好的”，不视为收尾授权。
用户直接指定的文档编写/修改任务本身就是对该范围的授权，不需要重复确认。

## 用户触发收尾后的检查清单

1. 核对本线程实际改动、当前实现及验证记录，区分已完成、部分完成、未验证和未完成。
2. 更新受影响的架构、流程、实现逻辑、代码地图及相关专题文档；修正旧描述，不只在末尾堆叠补充说明。
   有新增或重命名文档时，同步 `miscellaneous/project_docs_index.md` 的入口和链接。
3. 更新 `miscellaneous/待办.md`：仅勾选有实现和验证依据的事项；部分完成的条目拆分，保留未完成边界，
   记录新发现的问题与必要后续工作，不删除无关待办，也不把计划写成成果。
4. 验收记录写明实际执行的命令、结果和必要的日期/版本；没有运行的测试明确标注未验证。
   代码改动按风险进行构建、CTest 或实际渲染/UI 验证；仅文档修改检查内容一致性、链接和 diff 即可。
5. 检查文档与代码是否一致、链接是否有效，以及是否误改无关文件；用简短回复交代更新的文档、待办变化、验证结果和遗留项。

## 修改与提交边界

- 根目录 `README.md` 默认不修改；“收尾”或笼统的“更新文档”不包含它。只有用户明确要求修改 README 时例外。
- 保留用户或其他任务的已有改动，不覆盖、回退或顺手清理无关内容。
- 收尾不自动包含暂存、commit、push、建 PR 或归档任务；这些操作须有用户明确授权，并遵循用户指定的顺序与范围。
- 本文件是协作规则入口，不作为任务日志；项目状态写入对应文档，后续工作写入待办。
